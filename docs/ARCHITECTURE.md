# Logos Bible Study Chatbot — Detailed Architecture

Vachan Study (Logos Bible Study Chatbot) is a highly interactive, full-stack scripture study application with JWT-based authentication and strict single-session enforcement. It combines a modern Next.js frontend with a retrieval-first Python/FastAPI backend. The system is designed for conversational, multilingual, and voice-first interaction, using hybrid vector search to ground AI responses directly in scripture datasets. Deployed on Vercel with extensive production hardening.

---

## 🏗️ System Architecture Overview

### 1. Frontend Layer (Next.js + React)
The frontend provides a rich, responsive UI utilizing **Tailwind CSS** and **Framer Motion** for animations.

**Key Components (`src/components/Workspace.tsx`):**
- **Chat Interface:** Real-time streaming response UI using Server-Sent Events (SSE). Includes status updates (e.g., "Searching Scripture...", "Translating...").
- **Voice-First Input (STT):** Uses the `MediaRecorder` API to capture high-quality audio (`audio/webm;codecs=opus`), negotiates the best MIME type, and posts the audio blob to the backend for AI transcription.
- **Hands-Free Output (TTS):** Employs a custom "Autoplay Whitelisting" mechanism to defeat strict browser media blockers. Audio is streamed natively from the backend as pure Base64 JSON and loaded directly into a DOM-attached `<audio>` element.
- **Scripture Panel:** Displays the selected book (e.g., Genesis). Automatically highlights specific verses referenced by the AI during a chat.
- **Suggestion Chips:** Dynamic contextual follow-up questions generated by the AI based on the conversation context.
- **Settings & Help Modals (new):** Interactive `onOpenSettings` and `onOpenHelp` callbacks wired through `Workspace` → `Navbar` → `page.tsx`. Settings modal exposes theme toggling, API backend info, and version. Help modal provides a getting-started guide, voice feature docs, and keyboard shortcuts.
- **Q&A Dataset Viewer:** Fetches and displays the unfoldingWord translation questions per book from `/api/dataset/{book}` with search filtering.

### 2. Backend Layer (Python + FastAPI)
The backend manages all intelligence, orchestration, retrieval, and translation logic.

**Startup Behavior:**
- On cold start, logs whether `MONGO_URI` is configured and tests the MongoDB lazy connection object.
- Prints CORS configured origins for debugging.
- Warns if running in degraded mode (no DB).

**CORS & Security:**
- Production: locked to `https://vachan-study-chat-snpm.vercel.app` (always included).
- Preview deployments: dynamically adds the `VERCEL_URL`.
- Local development: allows `localhost:3000–3004` plus any extra origins from `ALLOWED_ORIGINS` env.
- **IP-based rate limiting**: In-memory store (`_ip_rate_store`), 30 requests per 60 seconds per IP on `/api/chat`. Resets per function cold start (acceptable for Vercel Free Tier).

**Key Endpoints (`backend/api/index.py`):**
- `GET /`: Root route — returns API info, version, and links to `/docs` and `/api/health`.
- `GET /api/health`: Health check with MongoDB `ping` command. Returns `healthy` or `degraded` status with connection details and timestamp.
- `POST /api/auth/register`: Creates new user accounts, storing hashed passwords in MongoDB.
- `POST /api/auth/login`: Validates credentials, issues JWT tokens, generates a new active session ID, and writes a `login_audit` record.
- `POST /api/auth/logout`: Invalidates the user's active session, logging them out globally.
- `GET /api/auth/me`: Authenticates JWT bearer tokens and returns the active user profile.
- `POST /api/chat`: The core intelligence loop. Protected by JWT authentication and IP rate limiting. Includes an **SSE timeout guard** (`MAX_DURATION = 8.0s` on Vercel, `30.0s` locally) with checks after every expensive operation. Processes history, runs language detection, performs hybrid search, formats RAG context, queries Gemini, and streams the response back via SSE.
- `POST /api/transcribe`: Receives raw audio blobs from the frontend, uses Gemini 2.5 Flash to generate highly accurate multilingual transcripts, and returns the parsed text.
- `POST /api/tts`: Receives text payload, auto-detects language (e.g., `ml` vs `en`), utilizes `gTTS` to generate raw MP3 streams, encodes the binary to a Base64 JSON payload, and returns it to the client to avoid HTTP chunk corruption.
- `GET /api/dataset/{book}`: Returns the offline Q&A dataset records for a given book (from TSV/CSV files).
- `GET/DELETE /api/history/{book}`: Manages user conversation history directly in MongoDB. Authenticated — scoped to the current user.
- `GET /api/scripture/{book}/{chapter}`: Fetches scripture text with a 3-layer fallback (MongoDB → API.Bible → placeholder). Adds `Cache-Control: public, max-age=3600` headers.
- **Global exception handler**: `@app.exception_handler(Exception)` catches all unhandled errors and returns structured JSON.

### 3. Data & Storage Layer (MongoDB Atlas)
The legacy local FAISS vector indices have been completely replaced with **MongoDB Atlas Vector Search** for scalable, cloud-native Hybrid Search.

**Collections:**
- **`users`**: Stores user authentication credentials. Fields include `username`, `email`, `password_hash` (hashed using bcrypt), and `session_id` (a dynamic token tracking the active session to enforce single-active-session concurrency limits).
- **`login_audit`** (new): Records each successful login with `username`, `timestamp`, and `status` for security auditing.
- **`qa_dataset`**: The core RAG knowledge base containing ~18,000 embedded Q&A pairs. 
  - Schema includes: `book`, `chapter`, `verse`, `question`, `answer`, `lang_code` (`en`, `ml`, `hi`), and `embedding`.
  - The `embedding` field contains exactly 768-dimension vectors (truncated from Gemini's native 3072 dimensions) using Matryoshka Representation Learning to save 75% storage space.
- **`chat_history`**: Persistent user conversation logs, organized by `session_id` and `book`.
- **`api_keys`**: Stateless Gemini token tracking. Synchronizes rate-limit `cooldown_until` timestamps across all parallel Vercel instances to prevent overlapping crashes.
- **`system_metrics`**: Global token usage tracking and API rate limits, perfectly synchronizing budget metrics across ephemeral serverless containers.

**Search Indices:**
- `vector_index`: Performs semantic similarity search on the `embedding` field.
- `text_index`: Performs BM25 keyword matching on the `question` field.

---

## 🧠 Intelligence Flow: Hybrid RAG Pipeline

When a user asks a question (e.g., "What did God create in the beginning?"):

1. **Language Detection & Translation:**
   - The user's query is analyzed using `langdetect`.
   - If the query is non-English (e.g., Malayalam), the system automatically translates the query to English to maximize search recall against the core dataset.

2. **Hybrid Search (`backend/services/retrieval.py`):**
   - The query is embedded into a 768-dimensional vector using Google's `text-embedding-004` model.
   - A concurrent MongoDB query executes both:
     - **Vector Search** (Semantic similarity)
     - **Text Search** (BM25 Keyword match)
   - Results from both algorithms are merged.

3. **Reciprocal Rank Fusion & Cross-Encoder Re-Ranking:**
   - The merged results are re-ranked using a Cross-Encoder (`cross-encoder/ms-marco-MiniLM-L-6-v2`).
   - The system establishes a strict confidence threshold. If a document scores high confidence (`>= 0.15`), it is classified as a "Direct Hit".

4. **Generation & Fallback Formulation:**
   - **Direct Hit:** The system instantly returns the pre-approved, dataset-backed answer without spending tokens on LLM generation, ensuring 100% theological accuracy.
   - **Low Confidence:** The system falls back to a Gemini AI prompt, feeding it the retrieved scripture context and asking it to generate a faithful answer.

5. **Native Translation Return:**
   - If the original query was non-English, the final English answer is translated back into the user's native tongue before being streamed to the frontend.

---

## 🛠️ Project Structure

```text
Logos Bible Study Chatbot/
├── .antigravity/          # IDE rule configurations
├── docs/                  # Architecture & TDD docs
├── GITHUB_ISSUES.md       # Vercel hosting audit & improvement tracker
├── public/                # Static assets
├── src/                   # Next.js Frontend
│   ├── app/               # Routing, layout, globals (Settings + Help modals)
│   │   └── page.tsx       # Main app with auth, modals, dataset viewer
│   ├── components/        # Workspace, Navbar, StudyRoom, LoginPage
│   └── data/              # Offline scripture fallback
└── backend/               # Python FastAPI
    ├── api/
    │   └── index.py       # Main router & all endpoints (consolidated)
    ├── app/core/          # Security (JWT, bcrypt), config (Pydantic settings)
    │   ├── config.py      # Application settings (SECRET_KEY, ALGORITHM, etc.)
    │   └── security.py    # Password hashing, token creation, rate limiting
    ├── config.py          # Environment & book mappings
    ├── db/
    │   ├── mongodb.py     # MongoDB Atlas connection (async Motor + sync PyMongo)
    │   ├── repositories.py # Data access: Chat, Scripture, Dataset, Keys, Metrics
    │   └── user_repository.py # User CRUD: create, get, session management
    ├── schemas/           # Pydantic request/response models
    ├── scripts/           # Migration & maintenance utilities
    ├── requirements.txt          # Vercel-safe (stripped — no faiss, langchain)
    ├── requirements-local.txt    # Full local dev (faiss, langchain, pytest, etc.)
    ├── runtime.txt               # Python 3.12 pinning for Vercel
    └── services/          # Business logic
        ├── ai_generation.py
        ├── embedding.py
        ├── key_rotation.py
        ├── rate_limiter.py
        ├── reranker.py
        ├── retrieval.py
        └── translation.py
```
