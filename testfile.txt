7TCfR3LK75PyT7yy

I need you to create a comprehensive test suite for my Logos Bible Study Chatbot application. 

## Project Structure:
- Frontend: Next.js (React) in `/frontend` directory
- Backend: Python FastAPI in `/backend` directory
- Database: MongoDB Atlas

## Tasks to complete:

### 1. Backend API Tests (FastAPI)
Create a test file at `/backend/tests/test_api.py` that tests:
- GET `/health` endpoint (should return 200)
- POST `/bible/ask` with a valid question like "What is justification by faith?"
- POST `/bible/ask` with empty question (should return 400 error)
- Test the MongoDB connection (mock it if needed)

### 2. Suggested Questions Dataset Test
Create a test at `/backend/tests/test_suggested.py` that:
- Verifies the `/suggested/questions` endpoint returns a list
- Verifies `/suggested/answer/{question_id}` returns correct answer
- Tests that clicking a suggested question returns exact match from dataset

### 3. Frontend Component Test
Create a simple test at `/frontend/components/__tests__/Chat.test.js` that:
- Checks if chat component renders correctly
- Simulates user typing a question
- Verifies suggested questions buttons appear

### 4. Local Model Integration Test (Ollama)
Create a script at `/backend/tests/test_local_model.py` that:
- Connects to Ollama at http://localhost:11434
- Sends a test prompt: "Explain Romans 8:28 in one sentence"
- Prints the response and measures response time

## Requirements:
- Use pytest for Python tests
- Use Jest + React Testing Library for frontend tests
- Include comments explaining each test
- Make tests simple and runnable on my system (i3, 12GB RAM)
- Don't require external API keys for tests

Please create all 4 test files and show me the code.